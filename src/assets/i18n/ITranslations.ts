export enum TranslationsRoot {
	ROOT = 'root'
}

export interface ITranslations {
	home: string;
	name: string;
	continue: string;
	avatar: string;
	lastName: string;
	email: string;
	configuration: string;
}
